#!/usr/bin/env ruby
#

# load the libs
$LOAD_PATH.unshift File.join(File.dirname(__FILE__),  '..', 'lib')

begin
  require 'reagan'
rescue LoadError => e
  raise "Missing lib #{e}"
end

Reagan.new.run
